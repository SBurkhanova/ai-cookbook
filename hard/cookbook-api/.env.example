# Copy to .env and fill in.
# - Without ANTHROPIC_API_KEY: DEMO mode (deterministic sample recipes).
# - Without MONGODB_URI: in-memory store (data resets on restart).
# - Without REDIS_URL: in-memory cache (data resets on restart).
ANTHROPIC_API_KEY=
MONGODB_URI=
REDIS_URL=
PORT=4000
CLIENT_ORIGIN=
